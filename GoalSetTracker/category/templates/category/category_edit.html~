{% extends 'login/base.html' %}

{% block content %}
<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title> Modify Category </title>
</head>
    <h1> Modify Category:</h1>
    <h3> Actual type of the category: {{category.category_tipo}}</h3>
  <body>
     <form action="{% url 'category_edit' category.id %}" method="post">{% csrf_token %}
        <p> New type <input name="category_tipo" size=40> </p>
        <p> Goal atached to category: </p>
        <p> Select the goal that you want to remove </p>
        <ul>
            {% for goal in category.goal.all %}
              <input type="checkbox" name="goal_rem[]" id="goal_rem{{ forloop.counter }}" value="{{ goal.id }}"/> 
              <label for="goal_rem{{ forloop.counter }}">{{ goal.goal_text }}</label><br />
            {% endfor %}
        </ul>
        <p> Goals that can be atached your category: </p>
        <p> Selec the goal that you want atach <p>
        <ul>
            {% for goal in goals_all %}
              <input type="checkbox" name="goal_add[]" id="goal_add{{ forloop.counter }}" value="{{ goal.id }}"/> 
              <label for="goal_add{{ forloop.counter }}">{{ goal.goal_text }}</label><br />
            {% endfor %}
        </ul>
        <p> Delete Category <input type="radio" name="deleted"> </p>
        <p> <input type="submit" value="Submit"> </p>
     </form>
  </body>
</html>
{% endblock %}
